# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
# Required by FastAPI's Form(...) parsing — api/oauth.py's /token
# endpoint takes application/x-www-form-urlencoded per RFC 6749 §4.1.3,
# not JSON.
python-multipart==0.0.12

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.29.0
alembic==1.13.3

# Auth & security
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
cryptography==43.0.3

# Settings
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0   # required by pydantic.EmailStr (app/schemas/user.py)

# HTTP client (for OPA calls)
httpx==0.27.2

# Revocation index (JTI blacklist) — see app/core/revocation.py
redis==5.2.0

# Observability
prometheus-fastapi-instrumentator==7.0.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Optional: SPIFFE workload identity
# pyspiffe==0.3.2
